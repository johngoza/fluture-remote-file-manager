version: "3.8"
services:
    ftp-server:
       build: ./tests/system/resources/ftp/
    sftp-server:
        build: ./tests/system/resources/sftp/
    test-server:
        build: .
        depends_on: 
            - ftp-server
            - sftp-server